# ImageAPI-Optimize-Vips

Plugin to use ImageApi Optimize Pipelines with vips.

It also supports creating webp and avif derivatives.

You need to install [libvips](https://github.com/libvips/libvips)

```bash
brew install vips
```
